function preload() {
    Attacks.preloadImages();
}
/*
function setup() {
    var canvas = createCanvas(width, height);
    canvas.parent('game');
}
*/
function draw() {
    background(100);
    //console.log("Card People being drawn");
    //Attacks[1].draw();
    //cardWaman.draw();
}